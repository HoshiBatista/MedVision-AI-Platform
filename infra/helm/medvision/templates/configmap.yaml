apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "medvision.fullname" . }}-config
  labels:
    {{- include "medvision.labels" . | nindent 4 }}
data:
  {{- include "medvision.appConfig" . | nindent 2 }}
