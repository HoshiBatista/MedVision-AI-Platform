apiVersion: v2
name: medvision
description: |
  MedVision AI Platform — medical imaging analysis (YOLOv11) with FastAPI
  microservices, Celery workers, local ONNX/Triton inference, and Ollama-backed
  clinical report generation. Mirrors the docker-compose dev stack.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Tracks the application/image tag shipped by default (see values.image.tag).
appVersion: "0.1.0"
keywords:
  - medical-imaging
  - yolov11
  - fastapi
  - ml-inference
maintainers:
  - name: MedVision AI
home: https://github.com/HoshiBatista/MedVision-AI-Platform
